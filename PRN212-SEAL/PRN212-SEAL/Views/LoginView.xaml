<UserControl x:Class="PRN212_SEAL.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F7FA">
        <StackPanel Width="350" VerticalAlignment="Center">
            <TextBlock Text="SEAL HACKATHON" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" TextAlignment="Center" Margin="0,0,0,30" />
            
            <TextBlock Text="ĐĂNG NHẬP" FontSize="20" FontWeight="SemiBold" Foreground="#34495E" TextAlignment="Center" Margin="0,0,0,20" />
            
            <TextBlock Text="Tên đăng nhập" Foreground="#7F8C8D" Margin="0,0,0,5" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15" />
            
            <TextBlock Text="Mật khẩu" Foreground="#7F8C8D" Margin="0,0,0,5" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,20" />
            <!-- Note: For real apps use PasswordBox, but TextBox is used here for simplicity in binding MVP -->
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#E74C3C" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,15" />
            
            <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Background="#3498DB" Foreground="White" Padding="10" FontWeight="Bold" Margin="0,0,0,15" Cursor="Hand" />
            
            <TextBlock TextAlignment="Center">
                <Run Text="Chưa có tài khoản? " />
                <Hyperlink Command="{Binding NavigateToRegisterCommand}">
                    <Run Text="Đăng ký ngay" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
