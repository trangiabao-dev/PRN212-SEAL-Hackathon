<UserControl x:Class="PRN212_SEAL.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Background="#F5F7FA">
        <StackPanel Width="350" VerticalAlignment="Center">
            <TextBlock Text="ĐĂNG KÝ TÀI KHOẢN" FontSize="20" FontWeight="SemiBold" Foreground="#34495E" TextAlignment="Center" Margin="0,0,0,20" />
            
            <TextBlock Text="Tên đăng nhập" Foreground="#7F8C8D" Margin="0,0,0,5" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,10" />
            
            <TextBlock Text="Mật khẩu" Foreground="#7F8C8D" Margin="0,0,0,5" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,10" />
            
            <TextBlock Text="Họ và tên" Foreground="#7F8C8D" Margin="0,0,0,5" />
            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,10" />
            
            <TextBlock Text="Email" Foreground="#7F8C8D" Margin="0,0,0,5" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,15" />
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#E74C3C" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,10" />
            <TextBlock Text="{Binding SuccessMessage}" Foreground="#2ECC71" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,10" />
            
            <Button Content="Đăng ký" Command="{Binding RegisterCommand}" Background="#2ECC71" Foreground="White" Padding="10" FontWeight="Bold" Margin="0,0,0,15" Cursor="Hand" />
            
            <TextBlock TextAlignment="Center">
                <Run Text="Đã có tài khoản? " />
                <Hyperlink Command="{Binding NavigateToLoginCommand}">
                    <Run Text="Về Đăng nhập" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
